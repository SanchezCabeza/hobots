% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_timeseries.R
\name{combine_timeseries}
\alias{combine_timeseries}
\title{Combine time series data from multiple CSV files into a single clean data frame}
\usage{
combine_timeseries(pattern = ".csv", date_col = "dateutc", value_col = "tem")
}
\arguments{
\item{pattern}{File name pattern to match (default: ".csv" for all CSV files).}

\item{date_col}{Name of the date column (default: "dateutc").}

\item{value_col}{Name of the value column (default: "tem").}
}
\value{
A cleaned data frame with unique, ordered dateutc records.
}
\description{
Reads all CSV files matching a pattern, combines into a single time series ordered by dateutc,
and removes duplicate dateutc records (keeping the first occurrence). Assumes the first column
is dateutc (\%Y/\%m/\%d \%H:\%M:\%S) and the second is tem. Handles malformed dates by attempting
alternative formats.
}
\examples{
\dontrun{
cleaned <- combine_timeseries(pattern = "mzt.*\\\\.csv$")
write.csv(cleaned, "mzt_combined_cleaned.csv", row.names = FALSE)
}
}
