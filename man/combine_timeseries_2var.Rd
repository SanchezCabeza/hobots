% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_timeseries_2var.R
\name{combine_timeseries_2var}
\alias{combine_timeseries_2var}
\title{Combine two-variable time series data from multiple CSV files}
\usage{
combine_timeseries_2var(
  pattern = ".csv",
  date_col = "dateutc",
  value_cols = c("oxy", "tem")
)
}
\arguments{
\item{pattern}{File name pattern to match (default: ".csv" for all CSV files).}

\item{date_col}{Name of the date column (default: "dateutc").}

\item{value_cols}{Character vector with the names of the two value columns (default: c("oxy", "tem")).}
}
\value{
A cleaned data frame with unique, ordered \code{dateutc} records and both variables.
}
\description{
Reads all CSV files matching a pattern, combines into a single time series ordered by dateutc,
and removes duplicate dateutc records (keeping the first occurrence). Assumes the first column
is a datetime field (\code{dateutc}, format: \\%Y/\\%m/\\%d \\%H:\\%M:\\%S) and the next two are the
variables of interest (e.g., \code{oxy} and \code{tem}). Handles malformed dates by attempting alternative formats.
}
\examples{
\dontrun{
cleaned <- combine_timeseries_2var(pattern = "mzt.*\\\\.csv$")
write.csv(cleaned, "mzt_combined_cleaned.csv", row.names = FALSE)
}
}
