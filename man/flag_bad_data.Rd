% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_bad_data.R
\name{flag_bad_data}
\alias{flag_bad_data}
\title{Flag bad data in a time series with continuous 30-minute intervals}
\usage{
flag_bad_data(
  df,
  temp_range = c(10, 40),
  z_threshold = 3,
  derivative_threshold = 0.1
)
}
\arguments{
\item{df}{Data frame with 'dateutc' (POSIXct) and 'tem' (numeric) columns.}

\item{temp_range}{Numeric vector of min/max plausible temperatures (default: c(10, 40)).}

\item{z_threshold}{Numeric z-score threshold for questionable data (default: 3).}

\item{jump_threshold}{Max allowed temp change per 30-min interval (default: 5).}

\item{flatline_threshold}{Max consecutive identical values (default: 5).}
}
\value{
Data frame with dateutc (character), tem, and tem.flag columns.
}
\description{
Creates a continuous 30-minute time series from a data frame with dateutc and tem.
Interpolates tem for gaps ≤30 minutes using linear interpolation, leaves gaps >30 minutes
as NA. Applies quality flags: 1 = Good, 2 = Quality Not Evaluated, 3 = Questionable/Suspect,
4 = Bad, 9 = Missing Data. Outputs dateutc as character (YYYY-MM-DD HH:MM:SS).
}
\examples{
\dontrun{
cleaned <- combine_timeseries(pattern = "mzt.*\\\\.csv$")
flagged <- flag_bad_data(cleaned)
write.csv(flagged, "mzt_flagged.csv", row.names = FALSE)
}
}
